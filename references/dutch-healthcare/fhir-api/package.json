{
  "name": "fhir-api",
  "scripts": {
    "deploy": "./node_modules/.bin/apigeetool deployproxy -u $APIGEE_USER -p $APIGEE_PASS -o $APIGEE_ORG -e $APIGEE_ENV -n $npm_package_name -V",
    "test": "./node_modules/.bin/cucumber-js test/integration/features"
  },
  "devDependencies": {
    "apickli": "^2.3.3",
    "apigeetool": "^0.14.2",
    "cucumber": "^6.0.5"
  }
}
