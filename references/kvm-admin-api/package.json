{
  "name": "kvm-admin-api",
  "version": "1.0.0",
  "description": "Proxy to administer KVM entries",
  "scripts": {
    "test": "npm i && npx cucumber-js --retry 10 --retry-tag-filter '@flaky' test"
  },
  "dependencies": {
    "@cucumber/cucumber": "^7.3.0",
    "apickli": "^3.0.1"
  }
}
